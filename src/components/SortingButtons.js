import React from "react";

export const selectionSort = (arr) => {
  console.log("selection Sort Excecuted");
  // console.log("ran");
  // console.log(arr);
  // for (let i = 0; i < arr.length; i++) {
  //   for (let j = i + 1; j < arr.length; j++) {
  //     if (arr[j] < arr[i]) {
  //       let temp = arr[j];
  //       arr[j] = arr[i];
  //       arr[i] = temp;
  //     }
  //   }
  // }
  // console.log(arr);
};
